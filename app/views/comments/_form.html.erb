<%= form_for(comment) do |f| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h4><%= comment.errors.count.to_s+"个错误" %> :</h4>

      <ul>
      <% comment.errors.full_messages.each do |msg| %>
        <li><%= format_error(msg) %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :content, :size => "68x9", :class => "input-xxlarge" %>
		<%= f.hidden_field :commable_type, :value => commable_type %>
		<%= f.hidden_field :commable_id, :value => commable_id %>
		
  </div>


  <div class="actions">
    <%= f.submit "回复", :class => "btn btn-primary ie6-btn" %>
  </div>

<% end %>

